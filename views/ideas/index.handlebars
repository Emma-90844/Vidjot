{{#each ideas}}
    <div class="card card-body mb-3">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a href="/ideas/edit/{{id}}" class="btn btn-secondary btn-block">Edit</a>
    </div>

{{else}}
    <p>No Video Listed</p>
{{/each}}






